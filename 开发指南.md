# AiPower 精简开发包

> 原项目大小：213MB → 精简后：208KB
> 生成时间：2026-09-03

---

## 一、项目结构总览

```
AiPower_slim/
├── apps/
│   └── aixsilicon-web/          # 前端主应用 (React + Vite + TypeScript + TailwindCSS)
├── packages/
│   ├── ui/                      # 共享 UI 组件库
│   └── shared-types/            # 共享 TypeScript 类型定义
├── backend/
│   └── app/
│       ├── schemas/             # API 接口定义 (Pydantic models)
│       ├── models/              # ORM 数据模型 (SQLAlchemy)
│       ├── api/                 # API 路由 (FastAPI endpoints)
│       └── core/                # 核心配置 (config, security, database)
├── docs/                        # 架构文档
├── package.json                 # 根依赖配置 (pnpm monorepo)
├── pnpm-workspace.yaml          # 工作空间配置
└── turbo.json                   # Turbo 构建配置
```

---

## 二、各模块详解

### 2.1 前端 (apps/aixsilicon-web/)

**技术栈：** React 18 + Vite + TypeScript + TailwindCSS + Radix UI

**目录结构：**
```
apps/aixsilicon-web/
├── src/
│   ├── api/                     # API 调用层（axios 封装）
│   ├── components/              # 页面组件
│   │   ├── common/              # 通用组件（Header, Sidebar 等）
│   │   ├── auth/                # 认证相关（Login, Register 等）
│   │   ├── tools/               # 工具市场相关
│   │   ├── files/               # 文件中心相关
│   │   └── ...
│   ├── hooks/                   # React 自定义 Hooks
│   ├── pages/                   # 页面路由组件
│   ├── stores/                  # 状态管理（Zustand/Context）
│   ├── types/                   # 前端 TypeScript 类型
│   ├── utils/                   # 工具函数
│   ├── App.tsx                  # 根组件
│   └── main.tsx                 # 入口文件
├── mock/                        # Mock 数据（开发阶段模拟 API）
├── index.html
├── package.json
├── vite.config.ts               # Vite 配置
├── tailwind.config.js           # TailwindCSS 配置
└── tsconfig.json
```

**关键配置文件：**
| 文件 | 作用 |
|------|------|
| `vite.config.ts` | Vite 开发服务器配置、代理设置 |
| `tailwind.config.js` | TailwindCSS 主题配置 |
| `.env.example` | 环境变量模板（API 地址等） |

**常用命令：**
```bash
pnpm install          # 安装依赖
pnpm dev              # 启动开发服务器
pnpm build            # 构建生产版本
pnpm lint             # 代码检查
pnpm typecheck        # TypeScript 类型检查
```

---

### 2.2 共享 UI 组件 (packages/ui/)

**作用：** 各应用共用的基础 UI 组件，避免重复开发

**组件列表：**
| 组件 | 说明 |
|------|------|
| `Button` | 按钮（多种变体：primary, secondary, ghost, danger） |
| `Card` | 卡片容器 |
| `Input` / `Textarea` | 输入框 |
| `Dialog` / `Modal` | 对话框/模态框 |
| `Table` | 表格（支持排序、分页） |
| `Tabs` | 标签页 |
| `DropdownMenu` | 下拉菜单 |
| `Badge` | 徽章 |
| `Avatar` | 头像 |
| `Switch` | 开关 |
| `Select` | 选择器 |
| `Checkbox` | 复选框 |
| `PopConfirm` | 气泡确认框 |

**使用方式：**
```tsx
import { Button, Card, Input } from '@ai-power/ui';
// 或
import { Button } from '@ai-power/ui/components/Button';
```

**依赖：** Radix UI primitives, TailwindCSS, class-variance-authority (cva)

---

### 2.3 共享类型 (packages/shared-types/)

**作用：** 前后端共用的 TypeScript 类型定义，确保类型一致性

**主要类型：**
```typescript
// project.ts
interface Project {
  id: string;
  name: string;
  description: string;
  created_at: string;
  updated_at: string;
}

// index.ts - 导出所有共享类型
export * from './project';
```

---

### 2.4 后端 API 定义 (backend/app/)

**技术栈：** Python FastAPI + SQLAlchemy (async) + Pydantic + PostgreSQL

#### 2.4.1 schemas/ - API 接口定义

**作用：** 定义 API 的请求/响应格式，前后端对接的核心依据

**文件说明：**
| 文件 | 定义内容 |
|------|----------|
| `auth.py` | 登录、注册、Token 相关 schema |
| `user.py` | 用户信息 schema |
| `tool.py` | 工具市场 schema |
| `file.py` | 文件中心 schema |
| `role.py` | 角色 schema |
| `permission.py` | 权限 schema |
| `category.py` | 分类 schema |

**示例（user.py）：**
```python
class UserBase(BaseModel):
    username: str
    email: EmailStr

class UserCreate(UserBase):
    password: str

class UserResponse(UserBase):
    id: int
    status: int
    is_superuser: bool
    created_at: datetime

    class Config:
        from_attributes = True
```

#### 2.4.2 models/ - ORM 模型

**作用：** 数据库表结构定义

**核心模型：**
| 模型 | 对应表 | 说明 |
|------|--------|------|
| `User` | users | 用户表 |
| `Tool` | tools | 工具表 |
| `Category` | categories | 分类表 |
| `FileAsset` | file_assets | 文件表 |
| `Role` | roles | 角色表 |
| `Permission` | permissions | 权限表 |
| `ToolUsage` | tool_usages | 工具使用记录 |
| `AuditLog` | audit_logs | 审计日志 |

#### 2.4.3 api/v1/ - API 路由

**作用：** 定义具体的 API 端点

| 文件 | 端点前缀 | 功能 |
|------|----------|------|
| `auth.py` | `/api/v1/auth` | 登录、刷新Token、登出 |
| `users.py` | `/api/v1/users` | 用户CRUD |
| `roles.py` | `/api/v1/roles` | 角色管理 |
| `permissions.py` | `/api/v1/permissions` | 权限管理 |
| `tools.py` | `/api/v1/tools` | 工具市场CRUD |
| `files.py` | `/api/v1/files` | 文件中心 |
| `categories.py` | `/api/v1/categories` | 分类管理 |
| `favorites.py` | `/api/v1/favorites` | 用户收藏 |
| `settings.py` | `/api/v1/settings` | 系统设置 |
| `sso.py` | `/api/v1/sso` | SSO登录 |
| `stats.py` | `/api/v1/stats` | 统计数据 |
| `exec.py` | `/api/v1/exec` | 工具执行 |
| `chat.py` | `/api/v1/chat` | 聊天功能 |
| `projects.py` | `/api/v1/projects` | 项目管理 |

**示例（auth.py 路由）：**
```python
@router.post("/login")
async def login(form_data: LoginForm) -> TokenResponse:
    """用户登录，返回 access_token 和 refresh_token"""

@router.get("/me")
async def get_current_user_info(current_user: User = Depends(get_current_user)) -> UserResponse:
    """获取当前登录用户信息"""
```

#### 2.4.4 core/ - 核心配置

| 文件 | 作用 |
|------|------|
| `config.py` | Pydantic Settings，环境变量配置 |
| `database.py` | 异步 SQLAlchemy 引擎和会话 |
| `security.py` | JWT Token 创建/验证，bcrypt 密码加密 |
| `redis_client.py` | 异步 Redis 客户端 |
| `permissions.py` | 权限检查装饰器 |
| `middleware.py` | 中间件（CORS 等） |

---

## 三、前端开发指南

### 3.1 环境准备

```bash
# 1. 安装 pnpm（如果没有）
npm install -g pnpm

# 2. 安装依赖
pnpm install

# 3. 复制环境变量
cd apps/aixsilicon-web
cp .env.example .env
# 编辑 .env，配置 API 地址
```

### 3.2 开发新页面

**步骤 1：在 pages/ 创建页面组件**
```tsx
// src/pages/MyNewPage.tsx
import { Card, Button } from '@ai-power/ui';

export function MyNewPage() {
  return (
    <Card>
      <h1>新页面</h1>
      <Button>点击</Button>
    </Card>
  );
}
```

**步骤 2：在路由中注册**
```tsx
// src/App.tsx 或路由配置文件
import { MyNewPage } from './pages/MyNewPage';

<Route path="/my-new-page" element={<MyNewPage />} />
```

**步骤 3：调用 API**
```tsx
import { api } from '@/api';

// 在组件中使用
const response = await api.get('/v1/tools');
```

### 3.3 调用后端 API

**已有 API 封装方式：**
```typescript
// src/api/index.ts
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000/api',
});

// 请求拦截器：添加 Token
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：处理 Token 过期
api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      // 尝试刷新 Token 或跳转登录
    }
    return Promise.reject(error);
  }
);

export default api;
```

**使用示例：**
```typescript
import api from '@/api';

export const toolApi = {
  list: (params: { page?: number; size?: number }) =>
    api.get('/v1/tools', { params }),

  getById: (id: string) =>
    api.get(`/v1/tools/${id}`),

  create: (data: CreateToolDto) =>
    api.post('/v1/tools', data),

  update: (id: string, data: UpdateToolDto) =>
    api.put(`/v1/tools/${id}`, data),

  delete: (id: string) =>
    api.delete(`/v1/tools/${id}`),
};
```

### 3.4 使用 Mock 数据

**文件位置：** `apps/aixsilicon-web/mock/`

```javascript
// mock/tools.ts
export default [
  {
    url: '/api/v1/tools',
    method: 'GET',
    response: () => ({
      code: 200,
      data: [
        { id: 1, name: '工具1', category: 'AI' },
        { id: 2, name: '工具2', category: '数据' },
      ],
    }),
  },
];
```

**Vite 配置中已开启 mock 插件，接口会优先被 mock 拦截。**

---

## 四、后端 API 参考

### 4.1 认证相关

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| POST | `/api/v1/auth/login` | 用户登录 | `{ username, password }` |
| POST | `/api/v1/auth/refresh` | 刷新 Token | `{ refresh_token }` |
| POST | `/api/v1/auth/logout` | 登出 | - |
| GET | `/api/v1/auth/me` | 获取当前用户 | - |

**登录响应示例：**
```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "eyJhbGc...",
  "token_type": "bearer"
}
```

### 4.2 工具市场

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/tools` | 获取工具列表（分页） |
| GET | `/api/v1/tools/{id}` | 获取工具详情 |
| POST | `/api/v1/tools` | 创建工具 |
| PUT | `/api/v1/tools/{id}` | 更新工具 |
| DELETE | `/api/v1/tools/{id}` | 删除工具 |
| POST | `/api/v1/tools/{id}/use` | 使用工具 |

### 4.3 文件中心

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/files` | 获取文件列表 |
| POST | `/api/v1/files/upload` | 上传文件 |
| GET | `/api/v1/files/{id}` | 获取文件详情 |
| DELETE | `/api/v1/files/{id}` | 删除文件 |
| GET | `/api/v1/files/{id}/download` | 下载文件 |

### 4.4 用户管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/users` | 获取用户列表 |
| GET | `/api/v1/users/{id}` | 获取用户详情 |
| PUT | `/api/v1/users/{id}` | 更新用户 |
| DELETE | `/api/v1/users/{id}` | 删除用户 |
| GET | `/api/v1/users/{id}/roles` | 获取用户角色 |

---

## 五、整合回完整项目

当你完成开发后，需要将修改整合回原项目时，按以下步骤操作：

### 5.1 前端修改

如果修改了 `apps/aixsilicon-web/src/` 下的文件：

```bash
# 原项目位置
cd D:\laptop\work\AI网站\AiPower-master-backup1-slim1\AiPower-master-backup1-slim

# 替换前端源码
# 将 slim_package/apps/aixsilicon-web/src/ 整个目录复制覆盖原目录
```

### 5.2 共享组件修改

如果修改了 `packages/ui/` 或 `packages/shared-types/`：

```bash
# 替换共享包
cp -r slim_package/packages/* 原项目目录/packages/
```

### 5.3 后端修改（如果涉及）

如果修改了 `backend/app/schemas/` 或 `backend/app/models/`：

```bash
# 替换后端定义文件
cp -r slim_package/backend/app/* 原项目目录/services/aixsilicon-api/app/
```

**注意：** 后端业务逻辑（`services/`、`repositories/`）不在此精简包中，如有修改需要单独处理。

### 5.4 重新构建

整合后，在原项目中执行：

```bash
# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 或构建生产版本
pnpm build
```

---

## 六、注意事项

1. **环境变量**：部署时确保配置正确的 `VITE_API_BASE_URL`
2. **Token 处理**：前端通过 `localStorage` 或 `cookie` 存储 Token
3. **CORS**：后端已配置 CORS，允许前端 origin 访问
4. **权限控制**：API 请求需要携带 `Authorization: Bearer <token>` 头
5. **Mock 模式**：开发阶段可使用 `mock/` 目录下的数据，不依赖后端

---

## 七、联系方式

如有问题，参考以下文档：
- `README.md` - 原项目说明
- `LOCAL_STARTUP_GUIDE.md` - 本地启动指南
- `docs/` 目录 - 架构设计文档
