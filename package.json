{
  "name": "aixsilicon-platform",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.13.4",
    "typescript": "^5.5.2"
  },
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
